interface:
  display_name: "TCX Judgment"
  short_description: "Independently challenge accepted investment artifacts"
  default_prompt: "Use $tcx-judgment to independently challenge accepted artifacts before downstream use."
policy:
  allow_implicit_invocation: true
