interface:
  display_name: Anti-Overfit Validation
  short_description: Backtest and signal validation against overfit risk
  default_prompt: Use $anti-overfit-validation to review signal quality.
policy:
  allow_implicit_invocation: true
