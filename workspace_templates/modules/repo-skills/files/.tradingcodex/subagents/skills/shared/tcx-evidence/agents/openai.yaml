interface:
  display_name: "TCX Evidence"
  short_description: "Collect source-backed evidence for analyst workflows"
  default_prompt: "Use $tcx-evidence to collect the smallest source-backed evidence set and choose only a justified evidence pack or role report when persistence is needed."
policy:
  allow_implicit_invocation: false
