interface:
  display_name: "TCX Build"
  short_description: "Update TCX and build broker providers"
  default_prompt: "Use $tcx-build for TradingCodex self-update, harness changes, or broker/API provider build work."
policy:
  allow_implicit_invocation: true
