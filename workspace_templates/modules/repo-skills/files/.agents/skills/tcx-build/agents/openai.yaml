interface:
  display_name: "TCX Build"
  short_description: "Mark one workspace-local build turn"
  default_prompt: "$tcx-build\nIf workspace files must change, use a workspace-write session. Describe the exact targets, reviewable diff, and validation required."
policy:
  allow_implicit_invocation: false
