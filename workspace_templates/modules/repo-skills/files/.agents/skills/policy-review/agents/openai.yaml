interface:
  display_name: "Policy Review"
  short_description: "Produce risk-manager policy readiness artifacts"
  default_prompt: "Use $policy-review only inside the risk-manager subagent."
policy:
  allow_implicit_invocation: false
