interface:
  display_name: "Execute Paper Order"
  short_description: "Submit approved orders through MCP"
  default_prompt: "Use $execute-paper-order to submit an approved paper order through the workspace MCP execution boundary."
policy:
  allow_implicit_invocation: false
