interface:
  display_name: "TCX Memory"
  short_description: "Replay decisions and validate lessons"
  default_prompt: "Use $tcx-memory to review a past decision, replay it with point-in-time evidence, and show what remains unvalidated."
policy:
  allow_implicit_invocation: false
