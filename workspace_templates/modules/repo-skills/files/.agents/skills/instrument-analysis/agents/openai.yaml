interface:
  display_name: "Instrument Analysis"
  short_description: "Review instrument mechanics and support boundaries"
  default_prompt: "Use $instrument-analysis to review instrument mechanics, source gaps, and support boundaries."
policy:
  allow_implicit_invocation: false
