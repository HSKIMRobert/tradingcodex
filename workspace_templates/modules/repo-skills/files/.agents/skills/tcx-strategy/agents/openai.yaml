interface:
  display_name: "TCX Strategy"
  short_description: "Author strategies through an explicit Build turn"
  default_prompt: "$tcx-build\nUse $tcx-strategy to create or update a user-approved strategy skill."
policy:
  allow_implicit_invocation: false
