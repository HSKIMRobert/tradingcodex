interface:
  display_name: "TCX Dashboard"
  short_description: "Summarize workspace state and attention items"
  default_prompt: "Use $tcx-dashboard to summarize what changed, what needs attention, and where to inspect it."
policy:
  allow_implicit_invocation: true
