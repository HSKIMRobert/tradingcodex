# Enforcer

The initial TradingCodex MCP keeps enforcer logic in the shared Python service layer and exposes stdio through `server.py`.

This directory marks the deterministic enforcement boundary for future extraction.
